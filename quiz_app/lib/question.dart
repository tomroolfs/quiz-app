class Question {
  String questionText;
  bool questionAnswer;

  Question({required String q, required a})
      : questionText = q,
        questionAnswer = a {
    questionText = q;
    questionAnswer = a;
  }
}

